doctype html
html
  head
    title Application Pending | Eporia Artist Portal
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    link(rel='stylesheet', href='https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&display=swap')
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css")
    style.
      * { margin: 0; padding: 0; box-sizing: border-box; }
      body { 
        font-family: 'Nunito', sans-serif; 
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
      }
      .pending-card {
        background: white;
        max-width: 600px;
        width: 100%;
        border-radius: 20px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.3);
        overflow: hidden;
        animation: slideUp 0.5s ease-out;
      }
      @keyframes slideUp {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
      }
      .card-header {
        background: linear-gradient(135deg, #88C9A1 0%, #5C4B3D 100%);
        padding: 40px;
        text-align: center;
        color: white;
      }
      .status-icon {
        width: 100px;
        height: 100px;
        margin: 0 auto 20px;
        background: rgba(255,255,255,0.2);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        animation: pulse 2s infinite;
      }
      @keyframes pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.05); }
      }
      .status-icon i {
        font-size: 50px;
      }
      .card-header h1 {
        font-size: 1.8rem;
        margin-bottom: 10px;
      }
      .card-header p {
        font-size: 1rem;
        opacity: 0.9;
      }
      .card-body {
        padding: 40px;
      }
      .timeline {
        margin: 30px 0;
      }
      .timeline-item {
        display: flex;
        gap: 20px;
        margin-bottom: 25px;
        position: relative;
      }
      .timeline-item:not(:last-child)::before {
        content: '';
        position: absolute;
        left: 17px;
        top: 40px;
        bottom: -25px;
        width: 2px;
        background: #E0E0E0;
      }
      .timeline-icon {
        width: 35px;
        height: 35px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        font-size: 0.9rem;
      }
      .timeline-icon.done {
        background: #88C9A1;
        color: white;
      }
      .timeline-icon.pending {
        background: #FFF3CD;
        color: #856404;
      }
      .timeline-icon.waiting {
        background: #F0F0F0;
        color: #999;
      }
      .timeline-content h3 {
        font-size: 1rem;
        margin-bottom: 5px;
        color: #333;
      }
      .timeline-content p {
        font-size: 0.9rem;
        color: #666;
        line-height: 1.5;
      }
      .info-box {
        background: #E8F5E9;
        border-left: 4px solid #88C9A1;
        padding: 20px;
        border-radius: 8px;
        margin: 30px 0;
      }
      .info-box h3 {
        color: #2E7D32;
        margin-bottom: 10px;
        font-size: 1rem;
      }
      .info-box p {
        color: #555;
        line-height: 1.6;
      }
      .contact-section {
        text-align: center;
        margin-top: 30px;
        padding-top: 30px;
        border-top: 2px dashed #EEE;
      }
      .contact-section p {
        color: #888;
        margin-bottom: 10px;
      }
      .contact-section a {
        color: #5C4B3D;
        font-weight: 700;
        text-decoration: none;
      }
      .contact-section a:hover {
        text-decoration: underline;
      }
      .btn-home {
        display: inline-block;
        margin-top: 20px;
        padding: 12px 30px;
        background: #5C4B3D;
        color: white;
        text-decoration: none;
        border-radius: 8px;
        font-weight: 700;
        transition: all 0.2s;
      }
      .btn-home:hover {
        background: #4A3D31;
        transform: translateY(-2px);
      }
      .rejected-box {
        background: #FFEBEE;
        border-left: 4px solid #E74C3C;
        padding: 20px;
        border-radius: 8px;
        margin: 30px 0;
      }
      .rejected-box h3 {
        color: #C62828;
        margin-bottom: 10px;
        font-size: 1rem;
      }
      .rejected-box p {
        color: #555;
        line-height: 1.6;
      }

  body
    .pending-card
      .card-header
        //- Status icon changes based on status
        if status === 'rejected'
          .status-icon
            i.fas.fa-times-circle
          h1 Application Not Approved
          p We were unable to verify your artist credentials
        else
          .status-icon
            i.fas.fa-hourglass-half
          h1 Application Under Review
          p Your artist profile is being verified by our team
      
      .card-body
        if status === 'rejected'
          //- REJECTED STATE
          .rejected-box
            h3 
              i.fas.fa-info-circle(style="margin-right:8px")
              | Reason for Rejection
            p= rejectionReason || "We were unable to verify the music ownership or identity information you provided."
          
          p(style="color:#666; margin-bottom:20px") If you believe this was a mistake or you'd like to reapply with additional verification information, please contact our team.
          
          .contact-section
            p Questions or want to appeal?
            a(href="mailto:artists@eporiamusic.com") artists@eporiamusic.com
            br
            a.btn-home(href="/") Return Home
        
        else
          //- PENDING STATE
          p(style="color:#666; margin-bottom:30px") Thank you for applying to become an Eporia artist! We're currently reviewing your application and verifying your artist credentials.
          
          .timeline
            .timeline-item
              .timeline-icon.done
                i.fas.fa-check
              .timeline-content
                h3 Application Submitted
                p You successfully submitted your artist application on #{appliedAt ? appliedAt.toLocaleDateString() : 'recently'}.
            
            .timeline-item
              .timeline-icon.pending
                i.fas.fa-sync-alt
              .timeline-content
                h3 Verification in Progress
                p Our team is reviewing your music platform links and verifying ownership of your discography.
            
            .timeline-item
              .timeline-icon.waiting
                i.fas.fa-headset
              .timeline-content
                h3 Identity Confirmation
                p We'll reach out via your preferred contact method to confirm your identity and discuss next steps.
            
            .timeline-item
              .timeline-icon.waiting
                i.fas.fa-rocket
              .timeline-content
                h3 Dashboard Activation
                p Once approved, you'll receive an email with login instructions and full access to your artist studio.
          
          .info-box
            h3 
              i.fas.fa-clock(style="margin-right:8px")
              | What to Expect
            p 
              | <strong>Review time:</strong> 2-3 business days on average. Applications with ISRC codes are prioritized.
              br
              br
              | <strong>Contact:</strong> We'll reach out to you via your selected verification method (email, Discord, Zoom, or phone) to complete identity verification.
              br
              br
              | <strong>Next steps:</strong> Once approved, you'll be able to upload your profile images, customize your bio, and start sharing music with the Eporia community.
          
          .contact-section
            p Questions about your application status?
            a(href="mailto:artists@eporiamusic.com") artists@eporiamusic.com
            br
            a.btn-home(href="/") Return Home

    script.
      // Optional: Poll for status updates
      const artistId = '#{artistId}'; // Pass from server
      
      if (artistId && '#{status}' === 'pending_review') {
        // Check status every 30 seconds
        setInterval(async () => {
          try {
            const response = await fetch(`/artist/api/check-approval-status/${artistId}`);
            const data = await response.json();
            
            if (data.approved) {
              // Redirect to dashboard
              window.location.href = '/artist/studio';
            } else if (data.status === 'rejected') {
              // Reload page to show rejection
              window.location.reload();
            }
          } catch (error) {
            console.error('Status check failed:', error);
          }
        }, 30000);
      }
